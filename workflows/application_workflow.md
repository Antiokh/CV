# Application Workflow — DEPRECATED

This file is retained only as historical context for the repository's older local `/applications/` workflow.

**Do not use it as an operational source for current employment applications.** In particular, do not use its old local-folder lifecycle, ZIP-pack standard, project-section requirements, compensation storage assumptions, or status model to drive WorkInterviews.

Current candidate-side employment operations are canonical in:

1. `GPT/GPT_RUNTIME.md`
2. `GPT/MODE_ROUTER.md`
3. `GPT/work-application-manager/SKILL.md`
4. `GPT/work-application-manager/references/tracker-storage-v5.md`
5. `GPT/work-application-manager/references/activity-log.md`
6. live hidden `Agent Instructions` in WorkInterviews

Resume writing itself is governed by `GPT/RESUME_ADAPTATION_WORKFLOW.md` and current evidence files.

Freelance/client work belongs to the separate Freelance/Agency workflow, not this historical employment file.
